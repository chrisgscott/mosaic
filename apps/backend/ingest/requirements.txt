# Core dependencies
supabase==2.3.4
gotrue==2.4.2
httpx==0.25.2
realtime==1.0.6
storage3==0.7.7
postgrest==0.15.1
python-dotenv==1.0.0

# Document processing - Docling (VLM-based)
# Fast, accurate document extraction with vision capabilities
docling  # Use latest version
transformers  # For HuggingFace tokenizers in HybridChunker
sentencepiece  # Required by some HuggingFace tokenizers
openai  # For API-based VLM (GPT-4o-mini)
pypdf  # For splitting PDFs into pages for parallel processing

# Text processing
langchain-text-splitters==0.3.4
tiktoken==0.8.0

# Database
psycopg2-binary==2.9.10

# Utilities
requests==2.32.3
