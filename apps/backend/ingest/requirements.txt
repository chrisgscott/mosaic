# Core dependencies
supabase==2.3.4
gotrue==2.4.2
httpx==0.25.2
realtime==1.0.6
storage3==0.7.7
postgrest==0.15.1
python-dotenv==1.0.0

# Document processing - Unstructured (OCR-based, legacy)
# Keep for backward compatibility during transition
unstructured==0.18.15
unstructured[pdf]==0.18.15
unstructured[docx]==0.18.15
unstructured[xlsx]==0.18.15
unstructured[pptx]==0.18.15
unstructured[csv]==0.18.15
unstructured[md]==0.18.15

# Document processing - Docling (VLM-based, new)
# Lightweight, faster alternative to Unstructured
docling  # Use latest version
openai  # For API-based VLM (GPT-4o-mini)
pypdf  # For splitting PDFs into pages for parallel processing

# Text processing
langchain-text-splitters==0.3.4
tiktoken==0.8.0

# Database
psycopg2-binary==2.9.10

# Utilities
requests==2.32.3
